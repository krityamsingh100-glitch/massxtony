version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-report-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json
      - ./logs:/app/logs
    environment:
      - TZ=UTC
